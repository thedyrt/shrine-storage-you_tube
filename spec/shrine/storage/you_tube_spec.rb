require 'spec_helper'

describe Shrine::Storage::YouTube do
  it 'has a version number' do
    expect(Shrine::Storage::YouTube::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
